# macOS / Windows
.DS_Store
Thumbs.db

# node
node_modules/

# środowisko / sekrety
.env
.env.*

# buildy
dist/
build/
.cache/

# kopie zapasowe schematow rozmow (tworzone przy edycji z poziomu appki)
*.js.bak

# baza Cold Calls: NIE w repo (2026-08-20 - byla tu, ale "git reset"/"git pull" na zywej
# bazie w trybie WAL nadpisal produkcyjne dane i pouciekaly hasla/leady/avatar - patrz
# server/backup.js, ktory teraz robi wlasne, bezpieczne kopie do data/backups/)
webapp/data/coldcall.db
*.db-shm
*.db-wal

# automatyczne kopie zapasowe bazy (patrz server/backup.js) - tworzone lokalnie, nie w repo
webapp/data/backups/

# Impeccable cache (stan sesji, nie kod)
.impeccable/hook.cache.json
**/.impeccable/hook.cache.json

# zrzuty ekranu robocze
screenshots/

# Local Netlify folder
.netlify

# Prywatne projekty, nie dla repo agencji
personal/
